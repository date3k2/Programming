#include <bits/stdc++.h>
#define ll long long
using namespace std;
int main() {
ios_base::sync_with_stdio(0);
cin.tie(0);
ll n;
cin>>n;
if (sqrt(n)==trunc(sqrt(n))) cout<<"YES"; else cout<<"NO";
  return 0; 
}